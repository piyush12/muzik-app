/*

    todo
        play / pause
        handle duration,
        handle volume control,
        handle seek change,
        handle repeat

    {
        currentDuration,
        totalDuration,
        setVolume,
        setSeekChange,
    }

*/

function useAudioPlayer(){

}

export default useAudioPlayer